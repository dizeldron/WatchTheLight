

public interface FractionCalculate {

    
    
  FractionalNumber Multiply(FractionalNumber first, FractionalNumber second);
  FractionalNumber Divide(FractionalNumber first, FractionalNumber second);
  FractionalNumber Append(FractionalNumber first, FractionalNumber second);
  FractionalNumber Subtract(FractionalNumber first, FractionalNumber second);
}
